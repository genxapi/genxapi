name: Publish Template Orval

on:
  push:
    tags:
      - "template-orval-v*"

permissions:
  contents: read
  id-token: write

jobs:
  publish-template-orval:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
      - name: Install dependencies
        run: npm install
      - name: Verify tag matches package version
        run: node scripts/verify-release-tag.mjs --workspace template-orval --tag-prefix template-orval-v
      - name: Build template
        run: npm run build --workspace "@genxapi/template-orval"
      - name: Run template tests
        run: npm run test --workspace "@genxapi/template-orval"
      - name: Pack dry-run
        run: npm pack --workspace "@genxapi/template-orval" --dry-run
      - name: Publish template
        run: npm publish --workspace "@genxapi/template-orval" --access public --provenance
